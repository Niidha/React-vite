import { Link } from "react-router-dom"

const ProfilePassword=()=>{
    return <div>
        Password update pages
        <Link to={"/settings/profile/update"}>Edit</Link>
    </div>
}
export default ProfilePassword